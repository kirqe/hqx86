<h5>comments:</h5>

<ul class="comments-list">
  <% commentable.cached_comments.each do |comment| %>
      <li class="comment" id="comment_<%=comment.id%>">
        <b>
          <% if current_user.try(:admin?) %>
              <%= link_to "-", [commentable,comment], method: :delete, data:{confirm: "Are you sure?"}, class:"delete", remote: true %>
          <% end %>

          <%= link_to "#{comment.user.try(:username)}:", user_path(comment.user)%>
        </b>

        <%= comment.body%>
      </li>
  <% end %>
</ul>
<br/>
